import { css } from "emotion";

export const container = css`
  background: rgba(0, 0, 0, 0.2);
  user-select: none;
  border-radius: 50%;
  height: 35px;
  cursor: pointer;
  width: 35px;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 10px;
  color: white;
`;
